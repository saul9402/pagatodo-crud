spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/pagatododb
    driver-class-name: org.postgresql.Driver